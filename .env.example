# Environment variables for local development
VITE_SUPABASE_URL=https://[project-id].supabase.co
VITE_SUPABASE_ANON_KEY=[anon-key]

# OpenRouteService API for real-time routing
# Get free API key from: https://openrouteservice.org/dev/#/signup
# NOTE: DO NOT use VITE_ prefix - it exposes keys in browser bundle
# Store as server-side env var in Vercel instead
ORS_API_KEY=your_openrouteservice_api_key

# Google Gemini API for AI features
# DO NOT use VITE_ prefix - it exposes keys in browser bundle
# Store as server-side env var in Vercel instead
# GEMINI_API_KEY=your_google_api_key

# Midtrans Payment Gateway (Sandbox/Production)
# Get credentials from: https://dashboard.midtrans.com/
# Server Key: Server-side only (used in API endpoints)
MIDTRANS_SERVER_KEY=SB-Mid-server-GkyMPSAEnrAzM1DMNzepISnB
MIDTRANS_MERCHANT_ID=G404807411
# Client Key: Can be exposed in frontend (VITE_ prefix OK)
VITE_MIDTRANS_CLIENT_KEY=SB-Mid-client-hdE4MHh7J0QtxgAn
# Environment: 'sandbox' or 'production'
VITE_MIDTRANS_ENVIRONMENT=sandbox
